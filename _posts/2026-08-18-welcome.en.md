---
title: "Welcome"
lang: en
# Jekyll only strips the .md extension from the filename when deriving
# the slug, so "welcome.en.md" would otherwise become the slug
# "welcome.en" and leak into the URL. Override explicitly — same value
# across every locale's file for this post.
slug: welcome
---

This is a sample post, editable from Decap CMS at `/admin/`. Delete once
you've published your first real one.
